# Разработка: объединяется с базовым compose (postgres, redis, сеть, тома).
#   docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build
# Инструменты (pgAdmin, Redis Commander):
#   docker compose -f docker-compose.yml -f docker-compose.dev.yml --profile tools up -d
#
# Backend: bind-mount ./apps/backend/app, uvicorn --reload.
# Frontend: Vite :5173 (FRONTEND_DEV_PORT), прокси на backend:8000 (VITE_DEV_PROXY_TARGET / vite.config.ts).
# Порты: !override — иначе при merge с docker-compose.yml останется ещё FRONTEND_PORT:80 (nginx), а в dev-образе слушается только 5173.

services:
  backend:
    build:
      context: .
      dockerfile: apps/backend/Dockerfile
      target: development
    volumes:
      - ./apps/backend/app:/app/app
    environment:
      GIGABOARD_IN_DOCKER: "1"
      DEBUG: "true"
      ENVIRONMENT: development
      SKIP_ALEMBIC_UPGRADE: ${SKIP_ALEMBIC_UPGRADE:-0}
    command: [ "uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload", "--proxy-headers", "--forwarded-allow-ips=*" ]

  frontend:
    build:
      context: .
      dockerfile: apps/web/Dockerfile.dev
    ports: !override
      - "${FRONTEND_DEV_PORT:-5173}:5173"
    volumes:
      - ./apps/web/src:/workspace/apps/web/src
      - ./apps/web/public:/workspace/apps/web/public
      - ./apps/web/scripts:/workspace/apps/web/scripts
      - ./apps/web/vite.config.ts:/workspace/apps/web/vite.config.ts
      - ./apps/web/tsconfig.json:/workspace/apps/web/tsconfig.json
      - ./apps/web/index.html:/workspace/apps/web/index.html
      - ./apps/web/postcss.config.cjs:/workspace/apps/web/postcss.config.cjs
      - ./apps/web/tailwind.config.js:/workspace/apps/web/tailwind.config.js
    environment:
      NODE_ENV: development
      VITE_DEV_PROXY_TARGET: http://backend:8000
    command: [ "npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173" ]
    depends_on:
      - backend

  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: gigaboard-pgadmin
    profiles:
      - tools
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_EMAIL:-admin@gigaboard.local}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_PASSWORD:-admin}
    ports:
      - "${PGADMIN_PORT:-5050}:80"
    depends_on:
      - postgres
    networks:
      - gigaboard-network
    restart: unless-stopped

  redis-commander:
    image: rediscommander/redis-commander:latest
    container_name: gigaboard-redis-commander
    profiles:
      - tools
    environment:
      REDIS_HOSTS: local:redis:6379
    ports:
      - "${REDIS_COMMANDER_PORT:-8081}:8081"
    depends_on:
      - redis
    networks:
      - gigaboard-network
    restart: unless-stopped
